node_modules
dist
coverage
bun.lock
bun.lockb
*.tsbuildinfo
.nuxt
.output
.nitro
.data

# Скиллы агентов: формат диктует спека Agent Skills, а не наш стиль. Prettier
# ломает YAML-фронтматтер и вложенные код-блоки шаблонов внутри SKILL.md.
.agents
.claude/skills

# Локальные настройки Claude Code: файл машинный, в git не попадает (он в
# глобальном gitignore) и пишется самим агентом при выдаче разрешений. Без этой
# строки `bun run check` падает на форматировании файла, которого нет в репозитории.
.claude/settings.local.json

# Артефакты MCP-сервера Playwright
.playwright-mcp

# Сгенерированный клиент Prisma
apps/api/generated

# Загруженные файлы встреч: среди форматов есть .md и .csv — Prettier принялся бы
# форматировать чужие файлы, а bun run check падал бы на них.
apps/api/.uploads
